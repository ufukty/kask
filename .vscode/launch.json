{
  "configurations": [
    {
      "name": "Docs site",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "./cmd/kask",
      "args": [
        "build",
        "-in",
        "${workspaceFolder}/docs",
        "-out",
        "${workspaceFolder}/docs-build",
        "-v",
        "-dev"
      ]
    }
  ]
}